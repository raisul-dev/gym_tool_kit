import 'package:flutter/material.dart';

class Gymtool extends StatelessWidget {
  const Gymtool({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: AppBar(title: Text('Gym Tool'),),
    );
  }
}